{-From https://codingbat.com/prob/p167011
Return an int array length 3 containing the first 3 digits of pi, {3, 1, 4}.
-}
import Control.Exception (assert)


makePi ::  -> [Int]
makePi  = undefined

main = do
    assert (makePi  == [3,1,4]) (putStrLn "Test passed")
    assert (makePi  == [3,1,4]) (putStrLn "Test passed")

